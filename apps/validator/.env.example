# ─── Validator ────────────────────────────────────────────────────────────────
# Your Solana wallet private key as a JSON byte array
# Generate with: solana-keygen new --outfile keypair.json
# Then paste the contents of keypair.json here
# ⚠️  NEVER share or commit this value — it controls your Solana wallet
PRIVATE_KEY=[1,2,3,...] 

# Hub WebSocket URL
HUB_URL=ws://localhost:8081
